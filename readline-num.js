const ask = () => {
  let readline = require('readline-sync');
  let a = readline.question('Give me the first number!');
  let b = readline.question('Give me the second number!');
};

module.exports = ask;
